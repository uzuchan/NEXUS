# NEXUS v1.1 "Pocket Universe" — 開発計画（オペレーター管理ファイル）

> このファイルは自動開発ループの**再開アンカー**。使用制限などでセッションが途切れたら、
> まずこのファイルの「進行状況」を読み、次のWaveから再開すること。

## ゴール

採用担当者がスマホでURLを開いても美しく・速く動くNEXUSにする。

### Definition of Done

1. **モバイル性能**: iPhone縦相当（390×844, DPR3エミュレーション）で平均45fps以上、UI崩れ・横スクロールなし
2. **アダプティブ品質**: FPS計測で high / mid / low の3段階を自動切替（粒子数・bloom解像度・DPRを段階制御）
3. **reduced-motion対応**: `prefers-reduced-motion` でカメラ演出・大型アニメーションを静的フォールバックに
4. **OGPリンクカード**: メール・チャットにURLを貼るとヒーロー画像つきカードが出る（`public/og.jpg` 1200×630 + meta）
5. **QA合格**: 契約違反ゼロ・consoleクリーン・headless Chromeモバイルエミュレーション実機検証 → 再撮影 → push公開

## 品質API契約（v1.1追加、全エージェント厳守）

- `SceneManager` が rolling FPS（約2秒窓、ヒステリシス3秒）で tier を決定: `<28 → 'low'`, `<45 → 'mid'`, それ以外 `'high'`
- `sceneManager.quality` で現在tierを公開。register済みオブジェクトに `setQuality(tier)` があれば tier変化時に呼ぶ（任意実装・後方互換）
- DPR: high=`min(devicePixelRatio,2)` / mid=1.5 / low=1（SceneManagerが所有）

## Wave編成（CLAUDE.md編成表に準拠、所有ファイル厳守）

| Wave | Agent | 所有ファイル | v1.1での任務 |
|---|---|---|---|
| 1 | 2 Scene Core | core/SceneManager.js | FPS計測・tier判定・setQuality配信・DPR段階制御・orientationchange対応 |
| 1 | 1 Design System | styles/tokens.css, styles/base.css | clamp()流体タイポ・safe-area変数・モバイル余白トークン・横overflow根絶 |
| 2 | 3 Particle FX | fx/Particles.js | setQuality実装: 粒子数 36K/18K/8K 段階制御 |
| 2 | 4 Environment | fx/Environment.js | setQuality実装: 構造体のセグメント/個数LOD |
| 2 | 5 PostFX | fx/PostFX.js | setQuality実装: bloom解像度スケール・low時bloom簡略化 |
| 2 | 6 Camera Director | motion/CameraDirector.js | 縦画面フレーミング補正・reduced-motion時は静的構図 |
| 3 | 7 Holo UI | styles/ui.css, index.htmlのsection内+head meta | カードのモバイルレイアウト・OGP/Twitterメタ追加 |
| 3 | 8 Interaction Lab | ui/interactions.js, styles/interactions.css | タッチ対応（tilt無効化→tap発光）・reduced-motion分岐・カスタムカーソルをタッチ時非表示 |
| 3 | 9 Narrative | content/copy.js | OGP用 title/description 文言（既存トーン維持） |
| 4 | 10 QA | (read + report) | 契約監査・モバイルエミュ実機検証・console確認 |
| 4 | Operator | main.js, public/og.jpg, 統合 | 配線・og.jpg生成（hero.jpgから1200×630切出し）・再撮影・commit/push |

## 進行状況

- [x] 計画策定・ゴール設定（2026-06-13 03:2x）
- [x] Wave 1: Scene Core + Design System 完了（build成功 / push済み）
  - 品質API確定: `sceneManager.quality`（getter）、register時に現tier即時通知、`setQuality(tier)` は `'high'|'mid'|'low'` 1引数・任意実装
  - DPRはSceneManagerが所有（high=min(dpr,2) / mid=1.5 / low=1）。Wave 2エージェントはDPRを直接触らないこと
  - **dvhへは移行しない（svh維持）**: スクロール総量が変わるとカメラドリー速度が揺れるため。Wave 2/3もこの前提を維持
- [x] Wave 2: 視覚4体 完了（build成功 / push済み）
  - Particles: drawRange方式。field/ember = high 36000/200, mid 18000/100, low 8000/44。high時の見た目不変
  - Environment: LODテーブルで段階削減。heroコアdissolve（smoothstep 2.6-4.4）は無傷、dispose適切
  - PostFX: bloom内部解像度 high 1.0 / mid 0.6 / low 0.4(strength 0.9倍)。resolution.setが効かないためtier変化時にbloomパス再生成。composer.setSize後に_sizeBloomで再適用しresize上書きを防止
  - Camera: 縦画面補正はFOV方式（カメラ位置不変＝dissolve距離に無干渉、基準16:9、上限100度）。reduced-motionは高速damp＋視差/揺れ停止、メディアクエリ動的変更に追従
- [x] Wave 3: UI/インタラクション/文言 3体 完了（build成功 / push済み）
  - Holo UI: OGP/Twitterメタ追加（og.jpgはWave 4で生成）、viewport-fit=cover、ヘッダーsafe-area対応、640px以下でカード1列・パディング縮小・HUDブラケット調整
  - Interaction Lab: pointer:coarseで判定しタッチ機ではカスタムカーソル/tilt/磁性ホバーを完全teardown、タップ発光パルスに置換。reduced-motionはreveal即時表示＋data-reveal即解放（QA修正のインバリアント維持）。メディアクエリ動的変更に追従
  - Narrative: copy.jsにogpミラー追加（index.htmlのmetaとロックステップ運用）
- [x] Wave 4: QA監査・実機検証・og.jpg・リリース 完了（2026-06-13）
  - og.jpg: hero.jpgから1200×630を生成し public/og.jpg に配置（dist同梱・HTTP 200確認済み）
  - QA判定: リリース可。BLOCKER/WARNゼロ、NIT3件（実害なし）。契約適合・dispose漏れなし・DPR二重制御なし
  - 実機検証: デスクトップ/iPhoneエミュともconsoleクリーン・横スクロールなし・reduced-motion即時表示・OGP整合・視覚破綻なし
  - README用メディアの再撮影は省略（high tierの視覚は設計どおり不変。QAスクショで確認済み）

## v1.1 リリース完了

全Wave完了。このプロジェクトの自動開発ループは終了し、以降の開発は新しいゴール設定から開始すること。

## 並行セッション注意（2026-06-13 03:26検出）

このリポジトリに別のClaude Codeセッションがコミットした形跡あり（メディア再生成・force-add）。
各Waveの前後で必ず `git pull --rebase` し、自分のWaveの所有ファイル以外の差分には触らないこと。

## 運用メモ

- 1ループサイクル = 1 Wave（投入→統合→`npm run build`確認→commit→push）
- リポジトリ名・ユーザー名・URLの変更は禁止（選考中の提出済みURL厳守）
- 使用制限で停止した場合: 毎時の自動再開チェック（このセッションのcron）が次の解除後にループを再開する
